# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. Extend the note dict to include a tags key (default []).  
Verify: existing tests still pass; creating a new note yields tags == []

2. Update the new‑note form to include a comma seprated “tags” input field.  
Verify: submitting “work, urgent” produces note["tags"] == ["work", "urgent"]

3. Add a tag‑parsing helper that splits on commas  and ignores empty strings
Verify: input " work , , school " becomes ["work", "school"].

4. Change note creating process to call the parser and store the resulting list in the note dict.  
Verify: manual POST creates notes with correct tag lists; existing testss still pass.

5. Update the notes list view to display tags as chips next to each note title.  
Verify: UI shows chips for notes with tags and shows nothing for notes with [].

6. Add a filter bar (“All / personal / work / …”) that sets a selected‑tag state.  
Verify: clicking a tag updates the filter state without errors

7. Update logic to show only notes whose tags contain the selected tag.  
Verify: selecting “work” hides all notes without "work" in their tag lst

8. Add tests for tag parsing 
Verify: tests pass for normal and empty imputs

9. Add tests for filter behavior (given notes with tags, filtering returns only matching notes)
Verify: selecting a tag returns the correct subset; “All” returns everything.

10. Run all tests  
Verify: all existing tests still pass; new tag/filter tests pass

## Task 1 context brief
Goal: add tags field to each note. New notes should default to tages=[]. Dont change any behavior
Constraints: notes are simple dicts, dont break existing text, tags must awalys exist on every note, and the default must be empty list
Acceptances: creating a new note makes {"title": "...", "body": "...", "tags": []}; existing tests all pass with no changes; no ai or process changes; nothing bad when reading notes without tags

## Task 1 — what happened
- Claude's plan summary:Here is Claude's plan:
╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌
 Plan: Add tags field to notes

 Context

 Notes are stored as in-memory dicts {"title":
  "...", "body": "..."}. The goal is to add a
 tags key that defaults to [] on every new
 note, without touching any existing behavior,
  tests, or templates.

 Change

 File: app.py, line 32

 # Before
 app.notes.append({"title": title, "body":
 body})

 # After
 app.notes.append({"title": title, "body":
 body, "tags": []})

 That's the only edit. No template changes, no
  test changes, no other behavior affected.

 Why this satisfies all constraints

 - New notes will have {"title": "...",
 "body": "...", "tags": []} ✓
 - Existing tests POST through the same
 endpoint, so they'll get notes with tags: []
 — none of the assertions check for the
 absence of tags, so they still pass ✓
 - Nothing reads note["tags"] anywhere (no
 templates, no routes), so notes without a
 tags key (e.g., notes seeded directly in a
 future test) won't cause crashes ✓
 - No AI, no process, no behavioral changes ✓

 Verification

 1. Run pytest — all existing tests must pass
 with no changes.
 2. Manually start python app.py, create a
 note, then inspect app.notes in a debug print
  or via a /notes JSON route (or just trust
 the test).
 3. Confirm app.notes[0] contains "tags": []
 after creation.
╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌

───────────────────────────────────────────────

     1. Run pytest — all existing tests must pass with no changes.
     2. Manually start python app.py, create a note, then inspect app.notes in a debug print or via a /notes JSON route (or just trust the
     test).
     3. Confirm app.notes[0] contains "tags": [] after creation.
- What I pushed back on: Making sure not to change any of the tests to make them pass
- Verification result: Ran tests myself and they passed except test_task_02 but thats because task 2 hasnt been implemented

## Task 2 — what happened
- ...

## Task 3 — what happened
- ...

## Reflection — where I redirected
- (3 sentences)

